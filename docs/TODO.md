# CloudNature プロジェクト TODO

最終更新: 2026-04-04

凡例: **完了** / **部分対応** / **スキップ** / **未着手**

---

## 計測・分析

- **未着手** KPI と計測設計を追加する
  - GA4 タグは導入済みだが、CV イベントの計測設計が未整備
  - 最低限: `お問い合わせ送信` `AI見積もりクリック` `サービスページ遷移` をイベント化
  - Search Console で追う対象 KW と対象 URL の対応表を作成
- **未着手** Search Console ベースの月次レビューを運用化する
  - 対象 KW ごとの掲載順位・表示回数・CTR・流入先 URL・問い合わせ寄与

## コンテンツ

- **部分対応** 事例本文の一次情報強化（業種・課題・導入前後の数値・期間を明記）
  - 業務自動化記事にて自社・支援先の一次情報を5箇所追加済み（週次レポート自動化、マーケ企業支援、AI見積もり等）
- **未着手** 業種別の導入事例を増やす（製造業 / サービス業 / 福祉 等、最低 3 本）
- **部分対応** ブログ記事の先行公開（3〜4 本）
  - [x] 「業務自動化の始め方」（`/usecases/business-automation-small-start`）— 2026-04-04 公開
  - [x] 「新潟×AI導入 補助金ガイド 2026年版」（`/usecases/niigata-ai-subsidy-guide-2026`）— 2026-04-04 公開
  - [ ] 「FAQ チャットボット 導入」
  - [ ] 「議事録 自動化 AI」
- **未着手** 訴求表現の根拠確認
  - `大手SIerの半額以下` `最短2週間` `24時間365日` `即日訪問対応` `全額返金保証`
  - 根拠が弱い場合は表現を弱めるか条件注記を追加（景品表示法リスク）

## SEO・メタデータ

- **完了** `estimate/app/chat/layout.tsx` 新規作成（`/chat` 専用 metadata）
- **完了** `estimate/app/layout.tsx` の Twitter title typo 修正
- **スキップ** `estimate/app/layout.tsx` の `keywords` フィールド削除 → 害がないため維持
- **スキップ** `app/layout.tsx` の Organization `sameAs` → SNS アカウントなし。開設時に再検討
- **部分対応** 各ページの title / description 精査 → `/usecases` 改善済み。他ページは概ね適切だが個別精査は未実施

## サイト構造・内部リンク

- **部分対応** `cloudnature.jp` 内部リンク再整理 → `/usecases` 専用 CTA 作成済み。サービス↔事例↔ユースケースは `RelatedLinks` で既存。残: ブログ→サービス導線（ブログ記事作成時に対応）
- **部分対応** `/cases` と `/usecases` の役割差を明確化 → usecases 側の CTA 分離 + description 改善済み。残: cases 側の metadata・本文設計の見直し
- **完了** `ai.cloudnature.jp` LP 本文の拡充 → `EstimateDetailsSection` 追加

## 構造化データ

- **未着手** `/cases` に `CollectionPage` または `ItemList` を追加
- **未着手** HowTo 構造化データの拡張（ブログ記事内の手順解説向け）
- **未着手** Service JSON-LD の補強（`serviceType` 等。`priceRange` は根拠確定後）

## コンテンツ（後回し）

- **未着手** 定義記事（`n8nとは`、`Difyとは` 等）— ドメイン権威性が低く上位表示困難
- **未着手** ニッチ KW 展開（`求人票 AI 自動生成`、`営業トーク AI` 等）— 検索ボリューム未確認
- **未着手** 技術スタック名での SEO 対策 — 採用ページ向け

## 外部施策

- **未着手** Google ビジネスプロフィールの最適化（カテゴリ・説明文を AI 導入支援寄りに更新）
- **未着手** 地域系の被リンク / PR 施策（NICO / DX パートナー掲載確認、地域メディア露出）

## AIEO（AI Engine Optimization）対策

ref: `docs/20260403_aieo_report.md`

構造化データ（FAQPage / JSON-LD）は実装済み。残課題はコンテンツ側の最適化。

### コンテンツ構造の最適化（高優先）

- **未着手** サービスページの見出しを質問形式に変更
  - 対象: `content/services.ts` の feature title
  - 例: 「社内ナレッジAIチャット」→「社内の問い合わせ対応をAIで自動化するには？」
  - LLM引用の44.2%はテキスト最初の30%から取られるため、導入文が重要
- **未着手** 回答ファーストのコンテンツ構造に変更
  - 各サービス説明・事例の冒頭1〜2文で端的に回答する形式にする
  - AIは「質問に直接答えている文章」を優先引用する
- **未着手** 具体的データの補強（出典・年付きの数値を追加）
  - 現状の「95%削減」「80%削減」は良いが、業界統計・調査データへの言及が不足
  - 「〇〇調査（2026年）によると…」パターンを追加

### 比較コンテンツの作成（中優先）

- **未着手** 「〇〇 vs △△」系の比較記事を1〜2本作成
  - [ ] 「AIエージェント開発 vs SaaS導入 — 中小企業にはどちらが最適？」
  - [ ] 「AI伴走支援 vs 自社内製化 — コスト比較」
  - 比較記事はAIに引用されやすいコンテンツの定番

### AI検索での現状把握（即実行可能）

- **未着手** Perplexity / ChatGPT で自社関連キーワードを検索し現状確認
  - 「CloudNature」「クラウドネイチャー」
  - 「新潟 AI導入 中小企業」「AI見積もり 自動化」
  - 現時点でのAIの回答を把握して対策の方向性を決める

---

## 運用・定期タスク

- [ ] Search Console で実際の検索クエリ確認（月次）
- [ ] 新規公開記事の検索順位確認（公開後 2〜4 週間、月次）
- [ ] キーワードプランナーでボリューム再確認（四半期）
- [ ] 競合 SERP 再調査 — NAB・NAIL 等の新規コンテンツチェック（半年）
- [ ] Perplexity / ChatGPT での自社引用状況確認（月次）
- [ ] AI引用→問い合わせの導線効果測定（四半期）
