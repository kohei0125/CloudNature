# /usecases/iphone-claude-ai-meeting-notes 記事レビュー・ファクトチェック

- 対象: `content/usecases/iphone-claude-ai-meeting-notes.ts`
- 設計書: `docs/blog/specs/20260823_ai_memo_iPhone.yaml`
- 実施日: 2026-08-23

## 検証目的

設計書（YAML）どおりの主張・トーン・構成で記事が書けているか、および
本文中の事実主張（サービス名・仕様・上限）が一次情報で裏付けられるかを確認する。

## 対象範囲

- 記事本文 HTML（`body`）と title / excerpt
- 使用画像 14 点（イラスト 6・実機スクリーンショット 8）
- `content/usecases/index.ts` への登録

## 確認項目リスト

### A. 設計書との整合

| 項目 | 結果 |
|---|---|
| core_message（追加契約なしで議事録環境を作れる）が本文で貫かれているか | OK |
| prohibited_claims（完全無料／専用サービス不要／100%正確 等）を使っていないか | OK・不使用 |
| terminology.avoid（半自動／完全自動ではない／価値の中心／二次的な選択肢） | OK・不使用 |
| pros-cons の explicitly_exclude（録音品質・完全自動ではない）を注意点に入れていないか | OK・除外済み |
| 競合4サービスを否定していないか（価格・精度・機能不足の批判なし） | OK |
| free_wording（「無料から試せる」等・「完全無料」不使用） | OK |
| 保存先を主役にしていないか | OK・`#prompt` 配下の h3 で簡潔に |
| 一人称の実践記トーン | OK |

### B. ファクトチェック（本文の事実主張）

| 主張 | 裏付け | 判定 |
|---|---|---|
| Claude iOS アプリはショートカットから「Ask Claude」で呼べる | Anthropic ヘルプセンター "Use Claude app intents, shortcuts, and widgets on iOS" | 裏付けあり |
| iOS 18 以上で利用可 | 同上（"available on iOS 18 and above"） | 裏付けあり |
| ショートカット経由の送信も利用上限に含まれる | 同上（"messages to Claude will count towards your overall usage limit"） | 裏付けあり |
| Notta / tl;dv / Genspark が AI議事録領域の既存サービスとして実在 | 各社の公開情報・比較記事で確認 | 裏付けあり |
| **ProudNote**（設計書の表記） | **該当サービスを確認できず。**実在するのは AI ボイスレコーダー **PLAUD NOTE**（jp.plaud.ai）で、読みが近く取り違えの可能性が高い | **設計書の表記を採用せず、PLAUD NOTE に置き換えた（要ユーザー確認）** |
| ショートカットの各アクション名・設定値 | 筆者の実機スクリーンショット 8 点（2026-08-23 撮影）と一致 | 裏付けあり |
| 「テキストに文字起こし」アクションの導入 iOS バージョン | 公式での明示を確認できず | **バージョンを断定せず「見当たらない場合は iOS を確認」と記載** |

### C. スキル規約チェック（Phase 15 相当）

| 項目 | 結果 |
|---|---|
| `npx tsc --noEmit` | 通過 |
| `npm run lint` | No issues found |
| `index.ts` 登録 | 済（配列先頭・import 順も一致） |
| 全画像に alt / width / height / loading="lazy" | 13/13 |
| 図解は h2 または h3 の直後 | OK |
| em ダッシュ「——」 | 0 件 |
| 冒頭フックあり・管理表へ追記 | 済（`docs/blog/guidelines/blog_opening_hooks.md`） |
| 200 字超の `<p>` | 0 件 |
| h2 の数 | 9 |
| 目次の件数・id・並び順が本文 h2 と一致 | 一致 |
| h4 不使用 | OK |
| 外部リンクの `target="_blank" rel="noopener noreferrer"` | 3/3 |
| CTA（AI見積もり・無料相談） | 2 箇所以上 |
| 内部リンク | 7 本 |
| 個人情報・顧客情報の写り込み | なし（ショートカット名「打合せ録音」のみ） |
| FAQ 3 問以内・本文と重複なし | OK |
| 監修・最終更新・出典ブロック | あり |
| モバイル（600px 幅）目視確認 | 実施・崩れなし |

## 未取得の素材（設計書 screenshots との差分）

設計書が挙げていた次の 3 点は素材が存在しないため、本文では文章のみで説明した。

- `shortcut-claude`（Claude へ送信するアクションの画面）
- `notion-result`（Notion に保存された実際の議事録）
- `home-icon`（ホーム画面に追加した打合せ録音アイコン）

素材が用意できた時点で `#prompt` / `#result` セクションに追加すると、実践記としての説得力が上がる。

## SEO（Ahrefs 実数値・JP・2026-08-23 取得）

| キーワード | Volume | KD | TP | Parent Topic |
|---|---:|---:|---:|---|
| 議事録 ai | 7,200 | 35 | 9,700 | ai議事録 |
| ai 議事録 | 2,700 | 35 | 85,000 | notta |
| ボイスメモ 文字起こし | 19,000 | 0 | 16,000 | ボイスメモ 文字起こし |
| iphone 文字起こし | 2,400 | 0 | 16,000 | ボイスメモ 文字起こし |
| iphone 録音 文字起こし | 1,600 | 2 | 16,000 | ボイスメモ 文字起こし |
| ai 議事録 無料 | 600 | 12 | 2,400 | 議事録 ai |
| 議事録 自動作成 | 500 | 2 | 900 | 議事録 自動作成 |
| iphone 議事録 | 150 | - | - | - |
| claude 議事録 | 100 | - | - | - |
| 議事録 自動化 | 90 | 3 | 800 | 議事録 自動作成 |

**判断**：主軸の「AI 議事録」は KD 35 かつ Parent Topic が `notta`（＝ツール比較・SaaS 公式が占有）で、
新規記事が短期に取れる見込みは低い。一方で「文字起こし」クラスタは KD 0〜2 で
ボリュームも大きく、本記事の内容（iPhone で録音して文字起こしする手順）と検索意図が一致する。

そのため設計書の推奨タイトル末尾「無料から始める方法」を **「文字起こしから保存まで」** に変更し、
title / H1 に「文字起こし」を含めた。「無料」は excerpt（meta description）側で
「無料から試せる構成です」として担保している。

- title: `iPhoneだけでAI議事録を自動化｜Claude×ショートカットで文字起こしから保存まで`
- slug: `/usecases/iphone-claude-ai-meeting-notes`（設計書どおり）

## Codex 対話レビュー（2026-08-23 / gpt-5.6-sol）

Codex から 8 件の指摘。対応状況は次のとおり。

| # | 指摘 | 判定 | 対応 |
|---|---|---|---|
| 1 | カレンダーのタイトル決定が説明と実装で食い違う。「候補として渡す」と書きながら、プロンプトは確定済みタイトルとして受け取る形。「外れたとき手入力」も実際は予定0件時のみ | **妥当** | 本文を修正。3件を**候補のまま**渡すことを明記し、プロンプトに「会議タイトルの決め方」ブロック（候補から選ぶ／該当なしなら内容から付ける）を追加。「タイトルを取得」が3件分を返す前提も追記 |
| 1b | 「新しい順3件」は現在時刻に近い順ではなく、午前の会議が候補から外れうる | **妥当** | 注記として明示（`※ 「新しい順」は開始時刻が遅い順なので…`） |
| 2 | 「保存まで公開」と書きながら、Claudeアクションの設定・保存アクション・ホーム画面追加の手順がない | **妥当** | h3「Claudeの返答を、次のアクションに渡す」を追加。標準の「メモに追加」を最短経路として提示し、Notion / Google Drive は同じ差し替えで対応できると説明。ホーム画面追加も1文追記 |
| 3 | 画像内コピー「専用アプリじゃなくていい」「追加の議事録サービス不要」が prohibited_claims「専用議事録サービスは不要」に接近 | **一部妥当・要ユーザー判断** | 画像は設計書 `images.assets` が指定したコピーそのもの（設計書内で矛盾）。**画像は差し替えず**、本文側の表現を「追加の契約を増やさずに始められる」に修正。画像コピーの扱いはユーザーへエスカレーション |
| 4 | 画像01の「Claudeで文字起こし＆議事録生成」が本文（文字起こしはiPhone）と矛盾 | **妥当** | alt は画像に忠実なまま、figcaption で実際の分担を明示。**画像自体の差し替えはユーザー判断待ち** |
| 5 | FAQ のデータ送信先の説明が Claude 分のみで、保存先が抜けている。「音声を送っていない」の主語が不明確 | **妥当** | FAQ を工程ごとの記述に書き換え（録音・文字起こし→iPhone／Claude→テキストのみ／保存先→議事録）。録音の事前告知を FAQ と注意点の両方に追加 |
| 6 | 「慣れていれば30分ほど」「直近3件あれば当たります」「iPhoneを置くだけ」など未実測の断定 | **妥当** | 「30分」を削除し筆者の実体験（数時間）に統一。「当たります」は候補方式の説明に置換。「置くだけ」→「机に置いてアイコンを押すだけ」 |
| 7 | 専用サービスの機能説明の主語が広すぎる（話者分離・自動参加はサービス／条件による） | **妥当** | 「サービスによっては、話者の識別や、オンライン会議への自動参加にも対応しています」に修正 |
| 8a | title 45文字は規約（30〜40文字）超過 | **妥当** | 「iPhoneだけで」→「iPhoneで」に短縮（全角換算 約37文字） |
| 8b | 保存先の h3 が `#prompt` 配下で目次から辿れない | **妥当** | h2 を「作り方3｜Claudeへ渡すプロンプトと、議事録の保存先」に改題（h2 を増やさず解決）。目次も同期 |
| 8c | FAQ の「無料プランでも作れるか」が本文（準備するもの・注意点）と重複 | **妥当** | 「ZoomやTeamsのオンライン会議でも使えますか？」に差し替え |

修正後に `npx tsc --noEmit` / `npm run lint` / 目次と h2 の一致 / 200字超段落 / em ダッシュを再チェックし、いずれも通過。

## ユーザーへのエスカレーション事項

1. **設計書の「ProudNote」**：実在を確認できず、PLAUD NOTE に置換した。意図が別サービス（Rimo Voice、CLOVA Note、LINE WORKS AiNote 等）なら差し替える。
2. **画像01（01-no-extra-service.webp）の記載内容**：「Claudeで文字起こし＆議事録生成」とあるが、実装では文字起こしは iPhone 側。図の作り直しを推奨。
3. **画像内コピーと prohibited_claims の整合**：設計書は「専用アプリじゃなくていい」「追加の議事録サービス不要」を画像コピーとして指定しつつ、prohibited_claims に「専用議事録サービスは不要」を挙げている。どちらを優先するか要判断。
4. **未取得素材**：Claude 送信アクション、Notion 保存結果、ホーム画面アイコンのスクリーンショット。
