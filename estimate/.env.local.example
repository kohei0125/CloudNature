# FastAPI バックエンドURL（サーバーサイドのみ）
BACKEND_URL=http://localhost:8000

# クライアント側APIベースURL（空 = 相対パス、本番では空のままでOK）
NEXT_PUBLIC_API_BASE=

# 環境識別（"production" 以外は noindex になる）
NEXT_PUBLIC_ENV=development

# backend へのAPIキー（本番環境で設定）
BACKEND_API_KEY=
