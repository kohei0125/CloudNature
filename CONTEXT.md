# CloudNature 用語集

AI見積もりシステム（`estimate/` + `backend/`）が扱う「概算お見積もり」とその成果物、およびコーポレートサイト（`app/` + `content/`）が掲げるサービス名称の用語集。

## Language

### サービス名称

3サービスの正式名称。表示順は `content/common.ts` の `SERVICE_ORDER`（`dev` → `ai` → `ai-support`）が単一ソース。

**システム開発**:
受発注・在庫・勤怠などの業務システムをオーダーメイドで構築し、既存システムとの連携・保守運用まで担うサービス。ページは `/services/system-dev`。

**AIエージェント開発**:
判断を伴うタスクを、人の管理下でAIが自律的に処理する仕組みを構築するサービス。ページは `/services/ai-agent`。

**法人向けAI導入支援**:
ツール導入で終わらせず、現場で使われる業務フローになるまで伴走するサービス。研修・セミナーを含む。ページは `/services/ai-support`。
_Avoid_: AI活用支援, AI導入支援（単独）, AI伴走支援
_Note_: `PAGE_META.servicesAiSupport` の title に含まれる「AI導入支援」は**検索語**であってサービス名ではない（ADR 0001 の帰結としてページ単位で温存している語）。名称として表示する箇所では「法人向けAI導入支援」を使う。

### 見積もりの価格モデル

**概算お見積もり**:
ユーザーの回答からPricing Engineが決定的に算出し、AIが文章化した概算。正式見積もりではなく、すべて**税別**で表示する。
_Avoid_: 正式見積もり, 確定見積もり

**ハイブリッド開発価格**:
CloudNatureが実際に提示する**主たる見積金額**。お見積書上の「御見積金額」「合計」はこの価格を指す。
_Avoid_: ハイブリッド料金（表記ゆれ）

**従来型開発価格**:
ハイブリッド開発の割安さを示すための**参考比較値**。お見積書では主役ではなく、混同を避けるため控えめに（参考注記として）扱う。明細表の主カラムには出さない。
_Avoid_: 通常価格, 定価（「参考値」であって定価ではない）

### 見積もりの確度

**お見積もり精度**:
回答の「未定」数などから決まる概算のブレ幅（±20% / ±30% / ±50%）。お見積書では顧客向けの項目として上部に明示する。`confidence.rangeLabel` に対応。
_Avoid_: 信頼度, 確度（表記ゆれ）

### 成果物

**お見積書 (PDF)**:
見積もり完了時に生成され、顧客メールに添付されるPDF。`estimate/components/pdf/EstimatePdf.tsx` がレンダリングする。法的・契約的効力を持つ正式書類ではなく、概算の提示物。

**確認事項 (discussion agenda)**:
正式見積もりに向けて顧客と詰めるべき論点のリスト。お見積書末尾に番号付きで掲載する。
_Avoid_: 課題, 宿題

## Flagged ambiguities

- **「合計金額」**: お見積書上の「合計」は常に**ハイブリッド開発価格・税別**を指す。従来型の合計は参考値であり、ここでの「合計」には含めない。
- **「AI活用支援」**: 正式名称ではない。2026-08-16 時点で `content/usecases/` 配下の公開記事7本（監修欄・提供領域の記述、計11箇所）に残っている。公開済みコンテンツのため一括改名は別タスクとする。

## 例: 開発者とドメイン担当の会話

> **Dev**: お見積書の明細表、従来型とハイブリッドの2列出してましたよね？
> **PO**: あれは混同のもと。明細表は**ハイブリッド開発価格**の1列だけにして。従来型は「ご参考」として小さく1行添えるくらいでいい。
> **Dev**: 「御見積金額」のバナーに出す数字は？
> **PO**: ハイブリッドの税別合計。うちが実際に出す金額はそれだから。従来型はあくまで“これだけ安い”を示す参考値。
