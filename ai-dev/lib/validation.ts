// クライアント側（フォームのmaxLength）とサーバー側（route.tsのバリデーション）で共有する上限値
export const MAX_SHORT_FIELD_LENGTH = 100; // name / company / role
export const MAX_EMAIL_LENGTH = 254;
export const MAX_TOPIC_LENGTH = 2000;
